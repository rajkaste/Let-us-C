#include<stdio.h>

int main(){

    int len, bth, area;
    printf("Enter the Length of rectangle: ");
    scanf("%d",&len);
    printf("\nEnter the breadth of rectangle: ");
    scanf("%d",&bth);

    area  = len * bth;
    printf("\nArea of Rectangle is: %d",area);

return 0;
}       